export interface ISet {
  home: {
    subheading: string;
  };
  lobby: {
    connectedPlayers: string;
    leaveLobby: string;
    startGame: string;
  };
  game: {
    submitExplanation: string;
  };
  settings: {
    language: string;
    save: string;
  };
}
